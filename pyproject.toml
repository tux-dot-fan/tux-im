[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tux-im"
version = "0.1.0"
description = "TUX IM — IBus input method engine for Pinyin, Wubi, Wbpy, and ASR"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "GPL-3.0-or-later" }
authors = [{ name = "TUX IM contributors" }]
keywords = ["ibus", "input-method", "pinyin", "wubi", "chinese", "asr"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: X11 Applications",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3.12",
    "Topic :: Text Processing :: Linguistic",
]
dependencies = [
    "PyGObject>=3.42",
    "httpx>=0.25",
    "sounddevice>=0.4.6",
    "tomli>=2.0; python_version < '3.11'",
    "tomli-w>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4",
    "pytest-asyncio>=0.21",
    "pytest-cov>=4.1",
    "ruff>=0.1",
    "mypy>=1.7",
    "respx>=0.21",
]

[project.scripts]
ibus-engine-tux-im = "tux_im.main:main"
tux-im-setup = "tux_im.ui.settings:main"

[project.urls]
Homepage = "https://github.com/tux-im/tux-im"
Documentation = "https://github.com/tux-im/tux-im/tree/main/docs"
Issues = "https://github.com/tux-im/tux-im/issues"

[tool.setuptools]
packages = [
    "tux_im",
    "tux_im.input",
    "tux_im.input.lexicon",
    "tux_im.asr",
    "tux_im.ui",
    "tux_im.config",
]
include-package-data = true

[tool.setuptools.package-data]
tux_im = ["py.typed"]

[tool.setuptools.data-files]
"share/ibus/component" = ["setup/com.github.tux-im.TuxIM.xml"]
"share/dbus-1/services" = ["setup/com.github.tux-im.TuxIM.service"]
"libexec" = ["setup/ibus-engine-tux-im"]
"share/applications" = ["setup/tux-im-setup.desktop"]
"share/tux-im" = ["data/*.yaml"]
"share/themes/TuxDark" = ["data/themes/TuxDark/index.theme", "data/themes/TuxDark/gtk-3.0/gtk.css"]
"share/themes/TuxLight" = ["data/themes/TuxLight/index.theme", "data/themes/TuxLight/gtk-3.0/gtk.css"]
"share/themes/TuxRetro" = ["data/themes/TuxRetro/index.theme", "data/themes/TuxRetro/gtk-3.0/gtk.css"]
"share/themes/Frost" = ["data/themes/Frost/index.theme", "data/themes/Frost/gtk-3.0/gtk.css"]
"share/themes/Forest" = ["data/themes/Forest/index.theme", "data/themes/Forest/gtk-3.0/gtk.css"]
"share/themes/Sunset" = ["data/themes/Sunset/index.theme", "data/themes/Sunset/gtk-3.0/gtk.css"]
"share/themes/Solarized" = ["data/themes/Solarized/index.theme", "data/themes/Solarized/gtk-3.0/gtk.css"]
"share/themes/Catppuccin" = ["data/themes/Catppuccin/index.theme", "data/themes/Catppuccin/gtk-3.0/gtk.css"]
"share/themes/Nord" = ["data/themes/Nord/index.theme", "data/themes/Nord/gtk-3.0/gtk.css"]
"share/themes/Dracula" = ["data/themes/Dracula/index.theme", "data/themes/Dracula/gtk-3.0/gtk.css"]
"share/themes/RosePine" = ["data/themes/RosePine/index.theme", "data/themes/RosePine/gtk-3.0/gtk.css"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "A", "C4", "PT", "RUF"]
# RUF001/RUF002/RUF003: ambiguous fullwidth characters (， （ ） etc.).
# RUF002/RUF003 fire in comments/docstrings — these appear intentionally
# in Chinese-language prose throughout this IME and forcing ASCII
# would make the Chinese text look wrong.
# RUF001 fires in plain strings — most often the `_ASCII_TO_CHINESE`
# mapping tables in tux_im/input/google_pinyin_mode.py and
# tux_im/input/wbpy.py, and their test fixtures, which store
# fullwidth punctuation by design.  Without this ignore, every
# fixture tuple in tests/test_wbpy.py / test_engine_flow.py /
# test_modifier_passthrough.py would lint-fail even though the
# mapping IS the contract being tested.
ignore = ["E501", "RUF001", "RUF002", "RUF003"]

[tool.ruff.lint.per-file-ignores]
# E402 on gi imports is intentional: gi.require_version() MUST come
# before `from gi.repository import ...`.  The `# noqa: E402` on the
# individual line is not enough because ruff's noqa scope is line-local
# and the `require_version` call is on the preceding line.
# Apply to all .py files; this is a global convention in the codebase.
"**/*.py" = ["E402"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unused_ignores = true
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["sounddevice", "sounddevice.*", "numpy", "numpy.*", "cairo", "cairo.*"]
follow_imports = "skip"

[[tool.mypy.overrides]]
module = ["tux_im.asr.*"]
ignore_errors = true

[[tool.mypy.overrides]]
module = ["tux_im.input.google_pinyin", "tux_im.input.google_pinyin_mode"]
ignore_errors = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-v --tb=short"
